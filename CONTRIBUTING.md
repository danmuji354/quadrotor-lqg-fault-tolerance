# Contributing

Add seeded tests for dynamics, estimation, controller, or fault-model changes. Run `ruff check src tests`, `pytest`, and the standard benchmark before submitting a focused pull request.

